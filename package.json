{
  "devDependencies": {
    "vitepress": "^1.5.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs --outDir dist-v1",
    "docs:build": "vitepress build docs --outDir dist-v1",
    "docs:preview": "vitepress preview docs --outDir dist-v1"
  }
}